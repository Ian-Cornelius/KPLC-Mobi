<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent">


    <EditText
        android:id="@+id/editOldPass"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:background="@drawable/ash_grey_edit_text_bg"
        android:ems="10"
        android:fontFamily="@font/lato_regular"
        android:hint="@string/old_pass"
        android:inputType="textPassword"
        android:padding="12dp"
        android:textColor="@color/custom_grey"
        android:textColorHint="@color/custom_grey"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/editNewPass"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:background="@drawable/ash_grey_edit_text_bg"
        android:ems="10"
        android:fontFamily="@font/lato_regular"
        android:hint="@string/new_pass"
        android:inputType="textPassword"
        android:padding="12dp"
        android:textColor="@color/custom_grey"
        android:textColorHint="@color/custom_grey"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="@+id/editOldPass"
        app:layout_constraintStart_toStartOf="@+id/editOldPass"
        app:layout_constraintTop_toBottomOf="@+id/editOldPass" />

    <Button
        android:id="@+id/btnChangePass"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="35dp"
        android:background="@android:color/transparent"
        android:drawableLeft="@drawable/change_icon"
        android:drawablePadding="9dp"
        android:fontFamily="@font/lato_regular"
        android:text="@string/change_pass"
        android:textAllCaps="false"
        android:textColor="@color/custom_grey"
        app:layout_constraintStart_toStartOf="@+id/editNewPass"
        app:layout_constraintTop_toBottomOf="@+id/editNewPass" />
</android.support.constraint.ConstraintLayout>