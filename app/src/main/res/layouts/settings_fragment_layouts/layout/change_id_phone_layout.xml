<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent">


    <EditText
        android:id="@+id/editOld"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:background="@drawable/ash_grey_edit_text_bg"
        android:ems="10"
        android:fontFamily="@font/lato_regular"
        android:hint="@string/old_id"
        android:inputType="number"
        android:padding="12dp"
        android:textColor="@color/custom_grey"
        android:textColorHint="@color/custom_grey"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/editNew"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:background="@drawable/ash_grey_edit_text_bg"
        android:ems="10"
        android:fontFamily="@font/lato_regular"
        android:hint="@string/new_id"
        android:inputType="number"
        android:padding="12dp"
        android:textColor="@color/custom_grey"
        android:textColorHint="@color/custom_grey"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="@+id/editOld"
        app:layout_constraintStart_toStartOf="@+id/editOld"
        app:layout_constraintTop_toBottomOf="@+id/editOld" />

    <Button
        android:id="@+id/btnChange"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="35dp"
        android:background="@android:color/transparent"
        android:drawableLeft="@drawable/change_icon"
        android:drawablePadding="9dp"
        android:fontFamily="@font/lato_regular"
        android:text="@string/change_id"
        android:textAllCaps="false"
        android:textColor="@color/custom_grey"
        app:layout_constraintStart_toStartOf="@+id/editNew"
        app:layout_constraintTop_toBottomOf="@+id/editNew" />
</android.support.constraint.ConstraintLayout>